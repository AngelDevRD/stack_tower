{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 _flutter && ./_flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'no install step needed'"
}
